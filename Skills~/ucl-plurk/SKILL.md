---
name: ucl-plurk
description: |
  對外發噗（Plurk）—— **走 `Cmd Plurk`，不再手動交給人貼**（Tim 2026-08-21 拍板；首則實跑
  `plurk_id 358451487782338`）。三步：`lint`（形式檢查）→ `preview`（組 payload 不送）→
  `post`（**要 `confirm=1` 才真送**，預設 dry-run）。
  交付單**五欄**：`persona / 心情詞 / 文案本體 / 圖片路徑 / 公開度` —— 公開度沒填直接被擋，
  ⛔ **不預設「所有人」**。
  **附圖已支援**（2026-08-21）：`圖片路徑` 填**絕對路徑**，post 會自動兩段式上傳並把圖片 URL 併進文案。
  ⚡ **自決發布授權**（Tim 2026-08-21）：Agent 自行依「我會不好意思 vs 有人被傷到」判準確認公開度與內容無虞、且 lint 通過後，**即可自帶 `confirm=1` 直接發出**，不需中斷流程向使用者確認；發出後回報 Plurk ID 與連結。
  ⚠ 機器只驗形式：**「這段被轉述出去，是我不好意思還是有人被傷到」lint 判不了** ——
  形式通過不等於可以發，那一格由發布者（人或具自決責任之 Agent）負責。
  觸發詞 (case-insensitive substring)：
  - **發文**：發噗 / 發一則噗 / 噗浪 / plurk / 對外發文 / 對外發布 / 貼到時間軸 / 發到噗浪
  - **交付**：交付單 / 文案本體 / 心情詞 / 公開度 / 只限朋友 / 偷偷說 / 匿名噗
  - **檢查**：發布前檢查 / 字數上限 / 300 字 / 超過拆兩則 / Plurk Paste / 拆成回應
  - **附圖**：附圖 / 貼圖 / 傳圖 / 上傳圖片 / 圖片路徑 / 帶圖發文 / uploadPicture
  - **表情**：自訂表情 / emoN / emo8 / 表情編號 / 表情表
  - **帳號**：共用帳號 / 公用帳號 / 個人帳號 / plurk 帳號 / plurk 憑證 / plurk token
  跨 agent 通用 —— Claude / Codex / Antigravity / Gemini 走同一支 Cmd 與同一份規則。
---

# UCL Plurk — 對外發噗

> 一句話：**規則長在發文那條路上，所以你不必記得它們** ——
> 但**公開度審查與 `confirm=1` 是人的，機器不接手**。

## 1. 三步（照順序，每步可單獨跑）

```bash
R="python <UCL_Core>/Tools~/AgentCommands/run_cmd.py --persona <me> run Plurk"
$R --arg op=resolve                                        # 用哪個帳號、憑證裝了沒
$R --arg op=lint    --arg slip_file=<交付單>                # 形式檢查（不連網）
$R --arg op=preview --arg slip_file=<交付單>                # 完整將送內容，**不送**
$R --arg op=post    --arg slip_file=<交付單> --arg confirm=1 # 真的發（有附圖會自動先上傳）
$R --arg op=upload  --arg image=<絕對路徑>   --arg confirm=1 # 只上傳一張圖、不發噗（驗端點用）
```

- 回傳檔在 `letters/<persona>/cmd/plurk_<op>.md` —— **Read run_cmd 印的那個路徑**。
- ⛔ **長文一律走 `slip_file`**，不要塞進 `--arg`（那條路會經過 shell，反引號與引號會被吃掉）。
- 拆則走回應：`--arg reply_to=<plurk id>` ⇒ 改打回應端點。

## 2. 交付單（五欄，欄序不拘、全形半形冒號都收）

```text
persona：basecamp
心情詞：說            # 覺得/說/想/哭/正在/分享/問/希望/愛/討厭/需要/有（表外一律退 says）
文案本體：
<純文案：首行自帶語氣、末行署名，一個字都不多>
—— basecamp ⛺
圖片路徑：D:/Unity/LY/AgentCommands/Canvas/canvas_latest.png   # ⛔ **絕對路徑**；沒有就填「無」
公開度：只限朋友       # 所有人 / 只限朋友 / 本人
```

## 3. lint 會擋什麼（每條底下都有一次真的出事）

| ✗ 擋下 | 血證 |
|---|---|
| 整行是括號註記 | 🩸 2026-08-07「（短、好笑、純自嘲）」**被當標題發出去** |
| 句內手動斷行 | 🩸 2026-08-11 手動斷行疊上 Plurk 軟斷行，句子被劈兩半 |
| 預算超 300（含 `[emoN]` 字面長度、附圖再保留 30） | 🩸 2026-08-16 稿子 365 字元、超線 5 個 —— Tim 先量出來的 |
| 共用帳號末行沒署名 | Tim 2026-08-16 硬規則：共用帳號讀者只看得到帳號 |
| **沒填公開度** | 逐篇公開度是現在就有的控制項；工具沉默地只發公開噗＝把它拿掉了 |
| 圖片路徑**不是絕對路徑**／檔案不存在 | 相對路徑會相對於 Editor 的工作目錄，同一份交付單換個地方跑就指到別的檔 |

| ⚠ 警告（不擋，但要你看） | 為什麼 |
|---|---|
| 含 `[emoN]` | **編號是快取、特徵才是事實** —— 面板重排就漂，且跨帳號號碼不同 |
| 第一行太短／幾乎只有表情 | 轉 Paste 或走回應時，時間軸上只看得到第一行 |
| 點名 `@某人` | 發前親自去講一聲（**已通知 ≠ 已讀**） |

## 4. 公開度審查與自決發布（Tim 2026-08-21 授權）

1. **公開度審查** —— 判準只有一句：
   > **「如果這段被轉述出去，問題是我會不好意思，還是有人被傷到？」**
   前者是坦白（可公開，**我負責**）；後者是隱私（不發）。
   ⚠ lint 每次都會印「本檢查不含公開度審查」—— 過了 lint 會被讀成過了審查，所以它必須自己講。
2. **`confirm=1` 與自決發布** ——
   - `confirm=1` 是 Cmd 的安全保護參數，發布不可回復，因此底層強制要求顯式傳入 `confirm=1`。
   - **Agent 自決發布**（Tim 2026-08-21）：當使用者指示發噗或 Agent 在任務／自由時間自決發噗時，只要 **`op=lint` 通過** 且 **Agent 自審符合上述公開度判準**，即可直接帶 `--arg confirm=1` 執行 `op=post` 發送，**不需額外向使用者詢問確認**；發布完成後回報 Plurk ID 與網址即可。

⛔ **匿名噗（偷偷說）預設不用**：它不是「更私密的一層」，是「公開但拿掉署名」——
拆掉的正好是判準裡「我負責」那一半。工具沒有實作它。

## 5. 超過 300 要拆：是兩半還是兩篇？（Tim 2026-08-21）

| 形態 | 什麼時候 |
|---|---|
| **回應（預設）** | 一篇被切成兩半 —— 後半離開前半讀不完整 |
| 兩則獨立噗 | **每則各有一個主題**、都能獨立被讀 |

分不出來 ⇒ 走預設（回應）。⚠ **署名兩則都要**；只切段落邊界，不切句內。

## 5.5 附圖（2026-08-21 起可用）

`圖片路徑` 填**絕對路徑** ⇒ `op=post` 會自己跑完兩段式：**先上傳** → 拿回圖片 URL →
**併進文案末行**（Plurk 那端渲染成 `<img>`）。你不必手動處理任何一步。

- 圖片 URL **吃 content 預算**（實測 50 字元）⇒ 附圖時 lint 的上限自動降為 `300 − 60 = 240`。
- `op=upload` 是**單獨驗端點**用的（只傳圖不發噗）—— 它也**要 `confirm=1`**，
  因為上傳本身就是對外寫入（CDN 上會留一張無主圖片）。
- ⚠ 上傳成功但併入後超長 ⇒ **噗不會發，而圖片已經在 CDN 上了**（清不掉）。
  所以文案先壓在 240 內，不要指望事後補救。
- ⚠ 圖裡有同事的作品／共創畫布／3D 展區 ⇒ 比照點名規則，**發前去酒館講一聲**。

## 6. 出事了 / 要改它 → 維護文件

本 skill 只講**怎麼操作 Cmd**。以下一律去
`ucl_core:Docs~/{lang}/Workflows/Plurk_Maintenance.md`，不在這裡重抄（重抄就會漂）：

| 想做什麼 | 那份文件的哪一節 |
|---|---|
| 加一條 lint 規則 / 加心情詞 | §2 / §3 |
| 設帳號、裝憑證、`.enc` 與明文的差別 | §4 |
| 呼叫回 4xx（簽章？端點？還是 WAF？） | §5「那個 403 不是他們擋 agent」＋ §6 |
| 端點與參數**驗證到哪了**（仍未驗清單） | §5（**事實來源在那裡，別在別處另記一份**） |
| 發完怎麼對帳（audit 台帳） | §7 |
| 改完 `.cs` 怎麼驗收 | §8 |

⚠ 三件最容易在操作時撞到、而答案在維護文件裡的事：
1. `op=resolve` 說 **`.enc` 有但明文沒安裝** ⇒ 還不能發（去 Secret Manager 做一次解密安裝）。
2. `[emoN]` **跨帳號號碼不同** —— 同一張圖在個人帳號與共用帳號是不同編號，以特徵為準。
3. 心情詞**不在表內會安靜退回 `says`** —— 想確認就跑 `op=preview` 看送出去的值。

## 延伸

| 想知道 | 看哪 |
|---|---|
| 發文流程完整版（排版三鐵律、表情對照表 16 項、治理約定、檢核清單） | `ucl_core:Docs~/{lang}/Workflows/Plurk_Posting_Workflow.md` |
| **維護／擴充**（架構分工、加規則、憑證、OAuth 坑、端點驗證狀態、對帳、驗收） | `ucl_core:Docs~/{lang}/Workflows/Plurk_Maintenance.md` |
| 帳號後台頁 | `ucl_core:Docs~/{lang}/UCL_EditorPage/UCL_PlurkAdminPage.md` |
| 設計沿革與分期（已完成） | `ucl_core:Docs~/{lang}/Plan/completed/Plan_Plurk_Bot.md` |

⛔ **規則只有 C# 這一份。** python `plurk.py` 只留 `resolve` / `whoami` 兩個**唯讀診斷**
（Editor 那條壞掉時的獨立第二條路），**刻意沒有 lint、也沒有發文** ——
規則放兩個語言各一份，發文那條路就繞得過其中一份，而繞過去不報錯。
